<%= simple_form_for(@age) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :found_id %>
    <%= f.input :item_id %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
